<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Crypto Future</title>
<style>
  /* FONT IMPORT */
  @import url('https://fonts.googleapis.com/css2?family=Inter:wght@500;700&display=swap');

  /* COLOR AND STYLE VARIABLES */
  :root {
    /* Main brand color */
    --brand-color: #a9d638;
    --brand-color-light: #c1f241;
    --brand-color-dark: #84a62c;
    
    /* Dedicated palette for the pie chart */
    --chart-color-1: #8a3ffc; /* Deep Purple */
    --chart-color-2: #41f2c9; /* Vibrant Cyan */
    --chart-color-3: #6ea5ff; /* Classic Blue */
    --chart-color-4: #f2c94c; /* Yellow Accent */

    /* UPDATED: Theme colors for transparency */
    --background-color: #000000;
    --card-color: rgba(28, 28, 28, 0.75); /* Semi-transparent */
    --card-benefits-color: rgba(34, 34, 34, 0.75); /* Semi-transparent */
    --border-color: rgba(169, 214, 56, 0.2);
    --border-hover-color: rgba(169, 214, 56, 0.6);
    --border-benefits-color: rgba(132, 166, 44, 0.4);
    --text-primary: #ffffff;
    --text-secondary: #b3b3b3;
  }

  /* GENERAL BODY STYLES */
  body {
    background-color: var(--background-color);
    font-family: 'Inter', sans-serif;
    color: var(--text-primary);
    margin: 0; /* Removed default margin */
    -webkit-font-smoothing: antialiased;
    -moz-osx-font-smoothing: grayscale;
  }
  
  /* --- Animated Background Canvas --- */
  #background-chart {
      position: fixed;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      z-index: -1; /* Place it behind all content */
  }

  /* LAYOUT WRAPPER */
  .wrapper {
    position: relative; /* Needed to stay on top of the fixed background */
    z-index: 1;
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 40px;
    max-width: 1200px;
    margin: 0 auto;
    align-items: stretch;
    padding: 80px 40px; /* Padding for the content area */
    box-sizing: border-box;
  }

  /* MAIN CARD STYLING */
  .content-block {
    text-align: left;
    background: var(--card-color);
    padding: 30px 40px;
    border-radius: 16px;
    border: 1px solid var(--border-color);
    transition: transform 0.3s ease, border-color 0.3s ease;
    display: flex;
    flex-direction: column;
    /* --- "Frosted Glass" Effect --- */
    backdrop-filter: blur(12px);
    -webkit-backdrop-filter: blur(12px);
  }

  .content-block:hover {
      transform: translateY(-5px);
      border-color: var(--border-hover-color);
  }

  /* TEXT SLIDE-UP ANIMATION */
  .animate-text-container { overflow: hidden; }
  .animated-text { transform: translateY(110%); transition: transform 0.6s cubic-bezier(0.2, 0.8, 0.2, 1); }
  .animated-text.visible { transform: translateY(0); }
  .animated-text.stagger { transition-delay: 0.15s; }

  /* GRAPHICS & SHAPES */
  .graphic-container { height: 350px; position: relative; display: flex; justify-content: center; align-items: center; margin-bottom: 30px; flex-grow: 1; overflow: hidden; }
  .shape { position: absolute; transition: transform 0.1s linear; }
  .stripe { width: 250%; height: 90px; background: linear-gradient(to right, var(--brand-color), var(--brand-color-dark)); transform: rotate(-35deg); }
  .sphere { width: 150px; height: 150px; background: radial-gradient(circle at 35% 35%, var(--brand-color-light) 0%, var(--brand-color-dark) 80%); border-radius: 50%; z-index: 2; }
  
  /* PIE CHART & LEGEND */
  .chart-layout-container { display: flex; align-items: center; justify-content: center; gap: 50px; flex-wrap: wrap; }
  .pie-chart { 
    width: 200px; height: 200px; border-radius: 50%; position: relative; 
    background-image: conic-gradient( 
        var(--chart-color-1) 0% 43%, var(--chart-color-2) 43% 71%,
        var(--chart-color-3) 71% 86%, var(--chart-color-4) 86% 100%
    ); 
    transform: rotateX(60deg) rotateZ(-30deg); 
    box-shadow: 0 18px 25px rgba(0,0,0,0.4); 
    flex-shrink: 0; 
  }
  .pie-chart::before { content: ''; position: absolute; top: 0; left: 0; right: 0; bottom: 0; border-radius: 50%; box-shadow: inset 0 0 15px rgba(0,0,0,0.5); }
  .chart-legend ul { list-style: none; padding: 0; margin: 0; font-size: 15px; text-align: left; }
  .chart-legend li { display: flex; align-items: center; margin-bottom: 12px; color: var(--text-secondary); }
  .chart-legend .legend-color { width: 16px; height: 16px; border-radius: 4px; margin-right: 12px; flex-shrink: 0; }

  /* BENEFITS BANNER */
  .benefits-card { grid-column: 1 / -1; margin-top: 40px; background-color: var(--card-benefits-color); border-color: var(--border-benefits-color); padding: 80px 60px; text-align: center; }
  .benefits-card .benefits-grid { display: flex; flex-direction: column; align-items: center; gap: 40px; }
  .benefits-card .benefits-row { display: flex; justify-content: center; gap: 100px; width: 100%; flex-wrap: wrap; }
  .benefits-card h2.benefits-title { font-size: 40px; font-weight: 700; margin-bottom: 60px; color: var(--brand-color); text-shadow: 0 0 15px rgba(169, 214, 56, 0.25); }
  .benefits-card .benefit-item { position: relative; padding-left: 25px; max-width: 300px; text-align: left; }
  .benefits-card .benefit-item::before { content: '•'; color: var(--text-primary); font-size: 16px; position: absolute; left: 0; top: 5px; }
  .benefits-card .benefit-item strong { display: block; font-size: 20px; font-weight: 700; color: var(--text-primary); margin-bottom: 10px; }
  .benefits-card .benefit-item .description { font-size: 20px; line-height: 1.7; color: var(--text-secondary); }

  /* GENERAL TEXT STYLES */
  h2 { font-size: 32px; font-weight: 700; margin: 0 0 15px 0; letter-spacing: -0.5px; text-shadow: 0 0 15px rgba(169, 214, 56, 0.2); }
  p { font-size: 16px; line-height: 1.7; margin: 0; color: var(--text-secondary); max-width: 450px; }
  .content-block:nth-child(2) p, .content-block:nth-child(2) h2 { margin-left: auto; margin-right: auto; text-align: center; }

  /* RESPONSIVE DESIGN */
  @media (max-width: 900px) {
    .wrapper { grid-template-columns: 1fr; gap: 40px; padding: 60px 20px; }
    .benefits-card { margin-top: 0; padding: 60px 40px; }
    .benefits-card .benefits-row { flex-direction: column; align-items: center; gap: 40px; }
  }
</style>
</head>
<body>

  <canvas id="background-chart"></canvas>

  <div class="wrapper">
      
    <!-- All content cards -->
    <div class="content-block">
      <div class="graphic-container">
        <div class="shape stripe"></div>
        <div class="shape sphere"></div>
      </div>
      <div class="animate-text-container">
        <h2 class="animated-text">Solid Foundations for Your Crypto Future</h2>
      </div>
      <div class="animate-text-container">
        <p class="animated-text stagger">Establish a solid foundation for your crypto journey through reliable and secure solutions.</p>
      </div>
    </div>
    <div class="content-block">
      <div class="graphic-container">
        <div class="chart-layout-container">
            <div class="pie-chart"></div>
            <div class="chart-legend">
              <ul>
                <li><span class="legend-color" style="background-color: var(--chart-color-1);"></span>Furniture: 43%</li>
                <li><span class="legend-color" style="background-color: var(--chart-color-2);"></span>Electronics: 28%</li>
                <li><span class="legend-color" style="background-color: var(--chart-color-3);"></span>Home Décor: 15%</li>
                <li><span class="legend-color" style="background-color: var(--chart-color-4);"></span>Toys: 14%</li>
              </ul>
            </div>
        </div>
      </div>
       <div class="animate-text-container">
        <h2 class="animated-text">Portfolio Distribution</h2>
       </div>
       <div class="animate-text-container">
        <p class="animated-text stagger">A clear overview of asset allocation, providing insight into your investment strategy and diversification.</p>
       </div>
    </div>
    <div class="content-block benefits-card">
      <div>
        <div class="animate-text-container">
          <h2 class="benefits-title animated-text">Benefit you get.</h2>
        </div>
        <div class="benefits-grid">
          <div class="benefits-row">
            <div class="animate-text-container">
              <div class="benefit-item animated-text stagger">
                  <strong>Proven Security:</strong>
                  <span class="description">Your assets are protected with industry-leading security protocols and robust infrastructure.</span>
              </div>
            </div>
            <div class="animate-text-container">
              <div class="benefit-item animated-text stagger">
                  <strong>Data-Driven Clarity:</strong>
                  <span class="description">Make informed decisions with transparent analytics and insightful portfolio tracking.</span>
              </div>
            </div>
          </div>
          <div class="benefits-row">
             <div class="animate-text-container">
                <div class="benefit-item animated-text stagger">
                    <strong>Future-Proof Growth:</strong>
                    <span class="description">Invest in a platform that is constantly innovating and built for long-term scalability.</span>
                </div>
            </div>
          </div>
        </div>
      </div>
    </div>

  </div>

  <script>
    // --- TEXT REVEAL ANIMATION ON SCROLL ---
    const observer = new IntersectionObserver((entries) => {
        entries.forEach((entry) => {
            if (entry.isIntersecting) {
                const textElements = entry.target.querySelectorAll('.animated-text');
                textElements.forEach(el => el.classList.add('visible'));
            }
        });
    }, { threshold: 0.5 });
    const animatedContainers = document.querySelectorAll('.content-block');
    animatedContainers.forEach((el) => observer.observe(el));

    // --- GRAPHIC ROTATION ON SCROLL ---
    const stripe = document.querySelector('.stripe');
    const sphere = document.querySelector('.sphere');
    const initialStripeRotation = -35;
    window.addEventListener('scroll', () => {
        const scrollTop = window.scrollY;
        const scrollRotation = scrollTop * 0.1;
        if (stripe && sphere) {
            stripe.style.transform = `rotate(${initialStripeRotation + scrollRotation}deg)`;
            sphere.style.transform = `rotate(${scrollRotation}deg)`;
        }
    });

    // --- CONTINUOUS BACKGROUND CHART ANIMATION ---
    const canvas = document.getElementById('background-chart');
    const ctx = canvas.getContext('2d');
    let points = [];
    let targetPoints = [];
    let lastUpdate = 0;
    const updateInterval = 2000; // Generate new points every 2 seconds

    function resizeCanvas() {
        canvas.width = window.innerWidth;
        canvas.height = window.innerHeight;
        initPoints();
    }

    function initPoints() {
        points = [];
        targetPoints = [];
        const pointCount = Math.floor(canvas.width / 40);
        for (let i = 0; i <= pointCount; i++) {
            const x = i * 40;
            const y = canvas.height * (0.4 + Math.random() * 0.4);
            points.push({ x, y });
            targetPoints.push({ x, y: canvas.height * (0.4 + Math.random() * 0.4) });
        }
    }
    
    function lerp(start, end, amt) {
        return (1 - amt) * start + amt * end;
    }

    function animate(timestamp) {
        ctx.clearRect(0, 0, canvas.width, canvas.height);
        
        for (let i = 0; i < points.length; i++) {
            points[i].y = lerp(points[i].y, targetPoints[i].y, 0.05);
        }

        if (timestamp - lastUpdate > updateInterval) {
            lastUpdate = timestamp;
            targetPoints.shift();
            targetPoints.push({
                x: targetPoints[targetPoints.length - 1].x, 
                y: canvas.height * (0.4 + Math.random() * 0.4)
            });
            for (let i = 0; i < targetPoints.length; i++) {
                targetPoints[i].x = i * 40;
            }
        }
        
        ctx.strokeStyle = 'rgba(0, 255, 80, 0.1)';
        ctx.lineWidth = 1;
        for (let i = 0; i < points.length; i++) {
            ctx.beginPath();
            ctx.moveTo(points[i].x, 0);
            ctx.lineTo(points[i].x, canvas.height);
            ctx.stroke();
        }

        ctx.beginPath();
        ctx.moveTo(points[0].x, points[0].y);
        for (let i = 1; i < points.length; i++) {
            ctx.lineTo(points[i].x, points[i].y);
        }
        ctx.strokeStyle = '#00ff50';
        ctx.lineWidth = 2.5;
        ctx.shadowColor = '#00ff50';
        ctx.shadowBlur = 20;
        ctx.stroke();
        ctx.shadowBlur = 0;

        requestAnimationFrame(animate);
    }
    
    window.addEventListener('resize', resizeCanvas);
    resizeCanvas();
    requestAnimationFrame(animate);

  </script>
</body>
</html>
